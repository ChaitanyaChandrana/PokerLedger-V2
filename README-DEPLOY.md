# Poker Ledger V2.6.18 — Blended Table Photo

The poker-table photo fades into the page on all four edges, with softer color
and no rounded card frame or border. The photograph remains beneath the player
list when details is closed; opening details shows the timeline.

## Deploy
1. Replace your existing index.html and sw.js with these files.
2. Publish normally, then refresh/reopen the app.
3. Confirm V2.6.18 in the home-screen footer.

The image is embedded. Keep all other site files and Firebase settings.
This update changes presentation only. CSS and package contents were inspected;
browser visual verification was not performed. Not deployed automatically.
